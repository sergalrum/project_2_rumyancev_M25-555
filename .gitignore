# === Виртуальные окружения (критично!) ===
.venv/
venv/
env/

# === Poetry-специфичные файлы ===
.cache/
# poetry.lock          # Раскомментируйте, если НЕ хотите хранить lock-файл в репозитории
                     # (рекомендуется оставить в репозитории для консистентности зависимостей)

# === Артефакты сборки и дистрибутива ===
dist/
build/
*.egg-info/
*.whl                # Игнорируем, если не храним готовые пакеты в репозитории
*.tar.gz            # Аналогично

# === Python-специфичные ===
__pycache__/
*.pyc
*.pyo
*.pyd
*.pyi              # Интерфейсные файлы mypy

# === Кэш инструментов анализа ===
.ruff_cache/
.mypy_cache/
.pytest_cache/
.coverage/
htmlcov/

# === Файлы данных и метаданных (НЕ должны быть в Git!) ===
data/                   # Все пользовательские данные (JSON-таблицы и метаданные)
data/*_meta.json      # Метаданные БД (например, db_meta.json)
data/*.json.backup    # Резервные копии JSON-файлов
data/*.json~          # Временные копии редакторов
data/*.log            # Логи внутри директории data/
data/*-backup.json   # Ручные дампы данных
data/*_dump_*.json    # Автоматические дампы данных
*.json.lock           # Блокировки JSON-файлов
*.json.swp            # Временные файлы Vim
*.json.swo            # Временные файлы Vim

# === Лог-файлы и временные данные ===
*.log
*.tmp
*.lock

# === Файлы IDE и редакторов ===
.vscode/
.idea/
*.sublime-workspace
*.swp
*.swo
*.swn

# === Системные файлы ОС ===
.DS_Store           # macOS
Thumbs.db          # Windows
ehthumbs.db
desktop.ini
._*                # Скрытые файлы macOS (resource forks)
.fuse_hidden*      # Скрытые файлы FUSE

# === Личные конфиги, секреты, окружение ===
.env
.env.*
*.local
secrets.py
config/local.py
.env.local           # Локальные настройки окружения
.secrets.yaml       # Секреты конфигурации
config/development.py # Настройки для разработки

# === Активы, сгенерированные в процессе разработки ===
asciinema-demo.json  # Демо-запись
demo.gif             # Конвертированное демо
screenshots/
*.png
*.jpg
*.jpeg

# === Тестовые артефакты ===
.tox/
.pytest/
test_output/
coverage.xml          # Отчёты о покрытии кода
junit-report.xml     # Отчёты тестов в формате JUnit
test-results/        # Результаты тестовых прогонов

# === Документация (если генерируется) ===
docs/_build/
docs/generated/
docs/build/          # Сгенерированная документация
site/               # Статический сайт документации (например, MkDocs)


# === Node.js (если используется фронтенд) ===
node_modules/
npm-debug.log
yarn-error.log

# === Docker (если используется) ===
.dockerignore
docker-compose.override.yml

# === Прочие временные файлы ===
*~                  # Файлы автосохранения редакторов
*.bak
*.old
*.orig

# === Временные файлы IDE и инструментов ===
.vscode/settings.json # Пользовательские настройки VS Code (если не нужны в репо)
.idea/workspace.xml  # Временные файлы IntelliJ
*.iml                # Файлы проекта IntelliJ

# === Логи и трейсы выполнения ===
logs/               # Директория для логов приложения
trace.log           # Файлы трассировки
debug.log           # Отладочные логи


# === Конфигурационные файлы (должны быть в Git, если требуется) ===
# !config/*.json       # Раскомментировать, если нужно включить конфигурационные JSON
# !schema.template.json # Раскомментировать, если нужен шаблон схемы БД